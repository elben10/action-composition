name: Call a reusable workflow

on:
  push:
    branches:
      - master

jobs:
  call-workflow-passing-data:
    uses: elben10/action-composition/.github/workflows/workflow-B.yml@master
    with:
      username: mona
    secrets:
      token: ${{ secrets.TOKEN }}