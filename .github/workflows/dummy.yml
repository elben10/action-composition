name: dummy

on: push

jobs:
  call-workflow-passing-data:
    env:
      one: 1
      two: zwei
      three: tres
    uses: elben10/action-composition/.github/workflows/stage_deploy.yml@master
    with:
      env_name: ${{ env.one }}